services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: tpgamesplus
      POSTGRES_USER: tpgamesplus
      POSTGRES_PASSWORD: tpgamesplus
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
